This fitgrid/binder folder is to isolate binder config files from the
rest of the repo, in particular environment.yml

The environment.yml must have what is necessary to run fitgrid but not
so specific that it doesn't run on the binder platform.





